import java.util.ArrayList;
import java.util.List;

class Heap {
    // List if integers to store integers that ref variables refer to 
    // (ref variables store index in this list)
    static List<Integer> listInt = new ArrayList<>();
}
